# Repository Guidelines

本文件定义本仓库 AI Agent 的工作边界、开发约束与验收标准。
目标：在不破坏现有光子设计流程的前提下，稳定迭代 `PhotonicsAI` 与相关脚本。

---

## Always（始终应用）

以下规则为**最高优先级**，在任何任务中始终生效：

1. 写任何代码前，必须完整阅读 `memory-bank/` 目录下所有文件。
2. 每完成一个重大功能或里程碑后，必须更新 `memory-bank/architecture.md`。
3. 每完成一步后，必须更新 `memory-bank/progress.md`。
4. 严格模块化，禁止单体巨文件。

---

## 1. Mission & Scope（目标与边界）

### 允许操作
- 读取、修改本仓库 Python 源码、配置、文档与测试文件
- 在当前项目环境中执行检查命令（如 `pytest`、`ruff`、`streamlit run`）
- 修复明确问题、补充必要文档、增加最小必要测试

### 禁止操作
- 未经明确要求进行大范围重构、目录迁移或 API 重命名
- 在代码中硬编码密钥、Token、账号或外部凭证
- 用 Mock/Stub/Demo 代码替代生产依赖的真实能力
- 基于猜测实现业务逻辑（需求不清先澄清）

### 高风险文件修改原则
- 对 `requirements*.txt`、`pyproject.toml`、核心流程文件（如 `PhotonicsAI/Photon/webapp.py`）做改动前，先确认影响范围
- 涉及仿真/版图产物（如 `.gds`、仿真 JSON）时，仅在任务明确要求下修改

---

## 2. 强前置条件约束（执行前必须满足）

1. 先理解需求与边界，再动代码；不允许“先改再说”
2. 先定位根因，禁止仅打补丁绕过问题
3. 优先复用现有模块与成熟依赖，避免重复实现
4. 不臆测接口行为，先查源码、配置或文档
5. 变更要最小化，禁止顺手改无关代码
6. 状态与数据源保持单一事实来源，避免多处维护同一事实
7. 异常必须显式处理，禁止吞异常或空 `except`
8. 代码必须可读：语义化命名、单一职责、避免过深嵌套

---

## 3. Tech Stack 与项目约定

### 核心技术栈
- Python 3.11+
- Photonics: `gdsfactory`, `gplugins`, `sax`, `tidy3d`
- App/服务: `streamlit`, `fastapi`, `uvicorn`
- AI/LLM: `openai`, `google-generativeai`, `transformers`, `sentence_transformers`
- 质量工具: `pytest`, `ruff`

### 代码组织规则
- 新逻辑优先放入现有模块边界，避免创建“万能文件”
- 函数应保持短小、单一职责、输入输出明确
- 文件名与符号命名必须语义清晰；Python 使用 `snake_case`
- 复杂流程补充必要注释，注释解释“为什么”，不重复“代码做了什么”

---

## 4. 环境与常见坑规避

### Python 环境
- 必须使用项目隔离环境（venv/conda 均可），避免全局污染
- 安装依赖后若 import 失败，先确认解释器与安装目标一致

### 常见问题处理
- 代码不生效：先检查保存、缓存、进程重启
- 依赖冲突：优先固定版本并记录原因
- API/模型报错：先校验环境变量和网络可达性
- 路径问题（Windows）：统一使用 `pathlib` 或规范化路径

---

## 5. 开发流程（Golden Path）

1. 明确任务边界与影响文件
2. 读取相关代码与配置，确认根因
3. 进行最小可行改动
4. 运行与改动最相关的验证（先局部，后整体）
5. 更新必要文档（README/AGENTS/模块文档）

---

## 6. 验收与提交标准

### 最低验收
- 代码可运行且改动点通过对应验证
- 不引入新的 lint/语法错误
- 不泄露敏感信息
- 文档与行为保持一致

### 建议命令
```bash
# 测试
pytest -q

# 代码质量
ruff check .

# Web 应用本地运行
streamlit run PhotonicsAI/Photon/webapp.py
```

---

## 7. 文档同步规则

以下内容变更后，必须同步文档：
- 运行命令、环境变量、依赖版本、目录结构
- 核心工作流步骤（提取、选型、DSL、版图/仿真、DRC）

不确定的信息使用“待确认”标记，不允许编造。
