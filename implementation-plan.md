# PhIDO 分步实施计划（Implementation Plan）

> 依据文档：`prd.md`、`tech-stack.md`  
> 原则：每步小而具体；每步都可验证；仅写执行指令，不写代码实现。

---

## 0. 执行规则

- 每完成一步再进入下一步，不并行推进关键路径。
- 每步必须记录：执行时间、结果、截图/日志路径。
- 若验证失败，先修复本步，不跳步。

---

## Phase 1：环境与基线（最小可运行）

### Step 1：确认 Python 版本
**指令**：在目标环境确认 Python 为 3.11.x。  
**验证方式**：运行版本检查命令，结果显示 `Python 3.11`。

### Step 2：确认当前解释器与项目环境一致
**指令**：检查 VS Code 解释器、终端解释器、`pip` 指向是否同一环境。  
**验证方式**：三者路径一致；不存在“装包成功但 import 失败”。

### Step 3：创建/启用隔离环境
**指令**：使用 conda 或 venv 启动项目专用环境。  
**验证方式**：环境名称清晰可见；新终端默认进入该环境。

### Step 4：安装核心最小依赖
**指令**：按 `tech-stack.md` 的“最小依赖基线”安装核心包。  
**验证方式**：关键包 `streamlit/gdsfactory/meep/openai/zhipuai` 均可导入。

### Step 5：安装系统级工具
**指令**：安装并配置 `KLayout`、`Graphviz`、`Meep`。  
**验证方式**：工具版本命令可返回版本号；命令在 PATH 可用。

### Step 6：配置环境变量
**指令**：创建 `.env`，至少设置 `ZHIPU_API_KEY`，并配置一个备份模型 Key（建议 OpenAI）。  
**验证方式**：应用启动后不出现“缺少 API Key”错误。

### Step 7：创建运行目录
**指令**：确认 `build/`、`log/` 目录可写。  
**验证方式**：目录存在且可写入测试文件。

### Step 8：运行静态检查基线
**指令**：执行 `ruff check .`。  
**验证方式**：无新增错误（允许历史问题，但要记录）。

### Step 9：运行测试基线
**指令**：执行 `pytest -q`（或先跑与工作流最相关测试集）。  
**验证方式**：记录通过/失败数；输出保存为基线报告。

### Step 10：启动应用基线
**指令**：运行 `streamlit run PhotonicsAI/Photon/webapp.py`。  
**验证方式**：页面正常打开；模型选择器、流程入口可见。

---

## Phase 2：工作流主链路验证（US-1）

### Step 11：准备标准输入样例
**指令**：使用 PRD 的标准输入（1×2 或 2×2 MZI + heater + 1550nm）。  
**验证方式**：输入文本存档在测试记录中，确保可复现。

### Step 12：验证 Step-1 实体提取
**指令**：在自动流程执行实体提取。  
**验证方式**：输出为结构化内容（JSON/YAML）；字段包含器件、参数、连接要素。

### Step 13：验证实体提取格式鲁棒性
**指令**：对同一需求改写 2 个同义表达重复执行。  
**验证方式**：三次结果语义一致，关键字段不缺失。

### Step 14：验证 Step-2 元件匹配（已存在元件）
**指令**：执行元件选型，目标命中已知元件（如 MZI heater 类）。  
**验证方式**：返回候选列表且包含预期元件；参数映射合理。

### Step 15：验证 Step-2 候选排序质量
**指令**：检查 Top-3 候选与需求一致性。  
**验证方式**：Top-1 与需求最匹配；若不匹配需记录原因。

### Step 16：验证 Step-3 原理图/DSL 生成
**指令**：生成 DSL 并查看拓扑结构。  
**验证方式**：端口连接完整、无悬空关键端口、格式可解析。

### Step 17：验证 Step-3 拓扑可视化
**指令**：生成并查看 DOT/图形预览。  
**验证方式**：节点数量与 DSL 实例数一致；连线逻辑正确。

### Step 18：验证 Step-4 布局生成
**指令**：执行版图生成并导出 GDS。  
**验证方式**：在 `build/` 产生新的 `.gds` 文件且大小 > 0。

### Step 19：验证 GDS 可打开性
**指令**：用 KLayout 打开 Step 18 的 GDS。  
**验证方式**：版图正常加载，无解析错误。

### Step 20：验证 Step-5 DRC 执行
**指令**：对生成 GDS 执行 DRC。  
**验证方式**：生成报告文件（如 `.lydrb`）；页面显示通过或违规明细。

### Step 21：验证端到端时延与稳定性
**指令**：完整自动流程重复执行 3 次。  
**验证方式**：成功率 >= 80%；平均耗时与 PRD 指标接近；失败原因可定位。

---

## Phase 3：关键能力专项验证

### Step 22：验证未知元件兜底生成（US-2）
**指令**：使用至少 3 条“库外需求”输入（例如异质集成激光器、非常规 MMI 比例器、超低损耦合器），逐条执行“意图解析 → 缓存检查 → 多源搜索 → 交叉验证”。  
**验证方式**：
- 每条请求都生成意图标签与关键参数清单
- 缓存命中/未命中状态可见
- 未命中时至少有 2 个外部来源证据进入候选列表
- 全流程无崩溃，失败时有明确错误原因

**执行子步骤**：
1) 准备 3 条低命中请求并编号保存。  
2) 先跑一轮，记录命中率与候选来源。  
3) 对同一请求再跑一轮，检查缓存复用行为。

### Step 23：验证动态元件可实例化
**指令**：对 Step 22 产出的每个候选组件，执行“约束融合（DRC + 物理 + 用户目标）→ 参数修正 → 组件生成 → 版图编译尝试”。  
**验证方式**：
- 至少 1 个候选可成功实例化并进入布局阶段
- 失败候选必须返回可读诊断（缺失参数、约束冲突、端口不兼容等）
- 输出包含“约束应用前后参数对比”记录

**执行子步骤**：
1) 导出候选参数表。  
2) 应用约束后重新排序候选。  
3) 对 Top-1/Top-2 执行实例化与版图编译测试。

### Step 24：验证自动仿真主线路（Meep）
**指令**：对“已存在标准元件 + 新生成元件”各选 1 个，执行 Meep 仿真，并进行敏感性分析（至少 2 个关键参数扰动）。  
**验证方式**：
- 两个样例均产出仿真结果（场分布或关键指标）
- 敏感性分析输出“参数影响排序”
- 日志包含仿真完成标记与关键指标摘要（如插损/带宽）

**执行子步骤**：
1) 设定基线参数。  
2) 对关键参数做小幅扰动并重复仿真。  
3) 记录性能变化并形成影响排序。

### Step 25：验证 Meep 深度仿真与反馈回写（US-3）
**指令**：对 Step 23 中最优新元件执行 Meep 深度仿真，并在结束后执行“结果返回 + 写入缓存 + 用户反馈采集”。  
**验证方式**：
- 输出场分布或等效仿真结果日志
- 超时时出现明确提示且流程可继续
- 结果对象包含缓存写入标识与版本号
- 用户反馈（评分/标签）被记录并可用于下一轮权重调整

**执行子步骤**：
1) 运行一次 Meep 并保存结果路径。  
2) 将结果与元件元数据写入缓存。  
3) 提交一条用户反馈并验证下一次候选排序变化。

### Step 26：验证 Tidy3D 备选路径（可选）
**指令**：验证系统中不存在可触发的 Tidy3D/SAX 分支入口，仅保留自动 Meep 主线路。  
**验证方式**：
- UI/配置中无 Tidy3D 与 SAX 可执行入口
- 运行日志不出现 Tidy3D/SAX 调用链
- 自动线路可正常完成仿真步骤

### Step 27：验证模板工作流（US-5）
**指令**：从模板启动并修改至少 2 个参数。  
**验证方式**：参数变更实际反映在 DSL/版图结果中。

### Step 28：验证自动线路回放一致性
**指令**：验证自动线路唯一性，重复执行同一请求 2 次（第二次应命中缓存并走自动回放）。  
**验证方式**：
- 第二次执行命中缓存并减少外部搜索/生成步骤
- 端到端输出与第一次语义一致
- 全程无需人工切换或手动注入

---

## Phase 4：健壮性与异常处理

### Step 29：验证主模型不可用时的处理
**指令**：临时禁用主模型 Key，仅保留备份模型。  
**验证方式**：流程可切换到备份模型，或给出明确降级提示。

### Step 30：验证 API 限流重试
**指令**：在高频调用下观察重试行为。  
**验证方式**：日志出现重试记录；最终结果符合成功或可读失败。

### Step 31：验证 KLayout 缺失场景
**指令**：模拟 `KLayout` 不可用环境执行 DRC。  
**验证方式**：提示“未安装/不可用”，应用不中断。

### Step 32：验证层兼容性处理
**指令**：使用可能触发层号冲突的样例。  
**验证方式**：出现映射或冲突日志；不会无提示失败。

### Step 33：验证输出目录保护
**指令**：检查生成文件是否仅写入约定目录（`build/`、`log/`）。  
**验证方式**：无越界写入；文件路径可追踪。

---

## Phase 5：质量门禁与发布准备

### Step 34：回归测试清单固化
**指令**：将通过的关键路径整理为固定回归清单（自动流、DRC、Meep 仿真、新元件闭环）。  
**验证方式**：清单可被他人复执行，结果一致。

### Step 35：建立最小验收标准
**指令**：按 PRD 指标设置“发布前必须满足”的 5 条硬门槛。  
**验证方式**：每条门槛有对应测试证据链接。

### Step 36：整理运行与排障文档
**指令**：同步更新 README/AGENTS 中的运行命令、环境变量、常见故障。  
**验证方式**：新成员按文档可在一台新机器跑通主流程。

### Step 37：发布候选版本（RC）
**指令**：打包当前可用版本并冻结依赖。  
**验证方式**：在干净环境复现安装与运行成功。

### Step 38：发布后观测项定义
**指令**：定义上线后 7 天内观测指标（成功率、耗时、错误类型 TopN）。  
**验证方式**：观测数据可按日输出，并可追溯到日志。

---

## 里程碑与完成定义（DoD）

### M1（环境就绪）
- 完成 Step 1–10
- DoD：应用可启动，最小依赖可运行，基线报告已存档

### M2（主链路跑通）
- 完成 Step 11–21
- DoD：自动五步流程稳定跑通，GDS + DRC 报告可产出

### M3（关键能力稳定）
- 完成 Step 22–33
- DoD：未知元件、仿真、异常场景均有可验证结果

### M4（可发布）
- 完成 Step 34–38
- DoD：回归门禁明确，文档同步完成，RC 可复现

---

## 执行记录模板（每步都要填）

- 步骤编号：
- 执行人：
- 执行时间：
- 输入：
- 输出：
- 验证结果（通过/失败）：
- 证据路径（日志/截图/报告文件）：
- 失败原因与修复动作：
